model_list:
  - model_name: gpt-3.5-turbo # Example model
    litellm_params:
      model: gpt-3.5-turbo
      api_key: "your-api-key-here"

general_settings:
  master_key: sk-1234 # Change this to a secure key
  cors_allow_origins: ["http://localhost:3005"] # ✅ CHANGED: Matches your backend port
  disable_ui: false # Ensure the UI is enabled if you are using the dashboard